{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "entities": ["dist/**/*.entity{.ts,.js}"],
    "synchronize": true
  }
  